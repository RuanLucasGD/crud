<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="utf-8">
    <title>CRUD de Clientes</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/js/script.js"></script>
</head>

<body>
    <div class="container">
        <h1>Lista de Clientes</h1>
        <% if (userAdded){ %>
            <h2>Usuario adicionado</h2>
            <% } %>
                <table>
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Telefone</th>
                            <th>UF</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <form method="post">
                            <% docs.forEach(doc=> { %>
                                <tr>
                                    <td>
                                        <%= doc.name %>
                                    </td>
                                    <td>
                                        <%= doc.telephone%>
                                    </td>
                                    <td>
                                        <%= doc.uf%>
                                    </td>
                                    <td>
                                        <button type="submit" onclick="setHiddenId('<%= doc._id %>')"
                                            formaction="/clients/update" class="btn-editar">Editar</button>
                                        <button type="submit" onclick="setHiddenId('<%= doc._id %>')"
                                            formaction="/clients/delete" class="btn-excluir">Excluir</button>
                                    </td>
                                </tr>
                                <% }); %>
                                    <input type="hidden" id="hiddenId" name="hiddenId">
                        </form>
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="4"><a href="/clients/add"><button type="button">Novo</button></a></td>
                        </tr>
                    </tfoot>
                </table>
    </div>
</body>

</html>